# Работы по проверкам качества кода

## 1. Конфигурация и зависимости

- [x] 1.1 Добавить в группу `dev` зависимости `ruff`, `mypy` и `types-PyYAML` с закреплёнными версиями (`==`). Проверка: `uv run --extra dev ruff --version` и `uv run --extra dev mypy --version` печатают именно закреплённые версии
- [x] 1.2 Добавить в `pyproject.toml` секцию `[tool.ruff]`: `line-length = 100`, целевая версия Python 3.12, набор правил `E`, `W`, `F`, `I`, `N`, `UP`, `B`, `C4`, `SIM`, `RET`, `RUF`, исключение сгенерированных каталогов. Проверка: `ruff check --statistics` выполняется и печатает перечень находок по правилам
- [x] 1.3 Добавить секцию `[tool.mypy]`: `strict = true`, `mypy_path = ["tools"]`, `explicit_package_bases = true`, переопределение `tests.*` без требования аннотировать сигнатуры, `ignore_missing_imports` для `pgserver`. Проверка: `mypy tools tests` запускается, разрешает импорт `garden_model` и печатает ошибки типов, а не ошибки конфигурации
- [x] 1.4 Зафиксировать исходный объём находок всех трёх инструментов до правки кода. Проверка: числа записаны в описание изменения и используются как точка отсчёта на шагах 3 и 4

## 2. Приведение оформления к норме

- [x] 2.1 Прогнать `ruff format` по всему проверяемому коду. Проверка: повторный `ruff format --check` завершается с кодом 0
- [x] 2.2 Перенести вручную комментарии, docstring'и и строковые литералы длиннее 100 символов — форматтер их не переносит. Проверка: `ruff check --select E501` не даёт находок
- [x] 2.3 Убедиться, что оформление не изменило поведение. Проверка: `uv run --extra dev pytest -q` зелёный, содержимое `changelog/` не изменено

## 3. Устранение находок линтера

- [x] 3.1 Применить автоисправления линтера. Проверка: `ruff check --fix` выполнен, `pytest -q` зелёный
- [x] 3.2 Устранить оставшиеся находки правкой кода: неиспользуемые импорты и имена, порядок импортов, устаревшие конструкции, ловушки `B`. Проверка: `ruff check` завершается без находок
- [x] 3.3 Там, где находка обоснованно подавляется, поставить точечное подавление с кодом правила и причиной. Проверка: в коде нет подавлений без кода правила, `ruff check` их не сообщает
- [x] 3.4 Убедиться, что правки ради линтера не изменили поведение. Проверка: `pytest -q` зелёный

## 4. Приведение кода к строгой проверке типов

- [x] 4.1 Проставить недостающие аннотации в `tools/garden_model`. Проверка: `mypy tools` не сообщает об отсутствующих аннотациях
- [x] 4.2 Сузить типы на границе разбора YAML в `model.py`: значения `Any` превращаются в типизированные структуры или в `ModelError`. Проверка: `mypy tools/garden_model/model.py` без ошибок, тесты формата модели зелёные
- [x] 4.3 Устранить оставшиеся ошибки типов в остальных модулях пакета и в `tools/host-runner/runner.py`. Проверка: `mypy tools` завершается без ошибок
- [x] 4.4 Устранить ошибки типов в тестах при действующем послаблении на аннотации сигнатур. Проверка: `mypy tools tests` завершается без ошибок
- [x] 4.5 Убедиться, что правки ради типов не изменили поведение. Проверка: `pytest -q` зелёный, сквозной тест сборки схемы с нуля проходит

## 5. Цели Makefile

- [x] 5.1 Добавить цели `format`, `lint` и `typecheck`, вызывающие инструменты через `uv run --extra dev`. Проверка: каждая цель запускается отдельно и даёт тот же вердикт, что прямой вызов инструмента
- [x] 5.2 Добавить цель `check`, вызывающую три под-цели рецептом через `$(MAKE)`. Проверка: `make check` на приведённом к норме коде завершается успехом, на файле с нарушением — ненулевым кодом возврата
- [x] 5.3 Убедиться, что новые секции укладываются в норму объёма. Проверка: `sh tools/make-lint/check.sh` завершается с кодом 0

## 6. Пайплайны

- [x] 6.1 Создать `.github/workflows/tests.yml`: прогон `pytest` на push в `main` и на предложения изменений, установка зависимостей через `uv`. Проверка: пайплайн запускается на предложении изменений и выполняет весь набор тестов, включая поднимающие временный PostgreSQL
- [x] 6.2 Создать `.github/workflows/quality.yml` с тремя раздельными шагами — `ruff format --check`, `ruff check`, `mypy` — выполняемыми все, даже если предыдущий отказал. Проверка: изменение с нарушением оформления и ошибкой типов одновременно показывает в логе обе находки за один прогон
- [x] 6.3 Удалить шаг прогона тестов из `.github/workflows/model.yml`, оставив в нём гейт выпуска модели. Проверка: в репозитории нет двух пайплайнов, прогоняющих тесты; гейт модели продолжает отрабатывать
- [x] 6.4 Проверить совпадение вердиктов. Проверка: на одном состоянии рабочего дерева `make check` и пайплайн качества дают одинаковый результат — оба успех либо оба отказ с теми же находками

## 7. Документация

- [x] 7.1 Описать порядок проверок: какие инструменты подключены, что проверяет каждый, как запускается локально и в каком пайплайне выполняется. Проверка: документ перечисляет все цели проверок и коды возврата
- [x] 7.2 Зафиксировать правило подавления находок и единственное послабление строгой проверки типов для тестов. Проверка: документ называет условия точечного подавления и причину послабления

## Версионирование

| Версия | Дата | Задача | Агент | Модель | Описание изменений |
|--------|------|--------|-------|--------|--------------------|
| 1.0.0 | 2026-09-15 | OpenSpec change `add-code-quality-pipeline` | Claude Code | Claude Opus 5 | Начальное создание: семь групп работ — конфигурация и зависимости, оформление, находки линтера, строгая проверка типов, цели `Makefile`, пайплайны, документация; каждая задача с указанием способа проверки |
